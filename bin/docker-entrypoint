#!/bin/bash -e

# If running the packaged app entrypoint then create or migrate the existing database first.
if [ "${1:-}" == "./bin/docker-start" ] || { [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; }; then
  ./bin/rails db:prepare
fi

exec "${@}"
