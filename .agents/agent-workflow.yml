---
base_branch: main
follow_up_prefix: "Follow-up:"
review_gate: "the full `gh pr checks` list green (not `--required`) + all review threads resolved + mergeable clean"
approval_exempt: "at batch closeout, auto-merge ready low-risk PRs that pass the merge gate; keep high-risk CI/workflow, build-config, dependency or runtime bumps, broad refactors, and release work maintainer-gated"
coordination_backend: "private shakacode/agent-coordination (claims/heartbeats namespaced by full repo name)"
changelog: "n/a"
benchmark_labels: "n/a"
merge_ledger: "n/a"
ci_parity_environment: "GitHub Actions `.github/workflows/ci.yml` (ubuntu-latest; scan_ruby, lint, and PostgreSQL/renderer test jobs)"
hosted_ci_trigger: "n/a — CI runs automatically on every PR; no manual trigger"
ci_change_detector: "n/a"
